# --- FastAPI ---
fastapi
uvicorn[standard]
alembic
fpdf2
python-multipart

# --- Database (PostgreSQL) ---
# Database (PostgreSQL)
sqlalchemy
psycopg[binary]  # PostgreSQL driver
psycopg2-binary

# --- Config & Security ---
# Config & Security
python-dotenv
pydantic-settings
python-dotenv
passlib[bcrypt]
python-jose[cryptography]

# --- PDF & Utils ---
fpdf2
python-multipart  # for form data (file uploads)

# --- Migrations ---
alembic
email-validator