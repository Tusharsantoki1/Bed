# --- FastAPI ---
fastapi
uvicorn[standard]

# --- Database (PostgreSQL) ---
sqlalchemy
psycopg[binary]  # PostgreSQL driver

# --- Config & Security ---
pydantic-settings
python-dotenv
passlib[bcrypt]
python-jose[cryptography]

# --- PDF & Utils ---
fpdf2
python-multipart  # for form data (file uploads)

# --- Migrations ---
alembic